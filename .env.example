# Copy this file to .env and fill in all values.
# NEVER commit .env to git.
#
# Generate DJANGO_SECRET_KEY:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

DJANGO_SECRET_KEY=your-secret-key-here

DEBUG=False
BROKER_ACCESS_CODE=your-access-code-here

# HTTPS / HSTS — set after HTTPS is verified end-to-end in production.
# Enabling HSTS prematurely will lock users out of HTTP permanently.
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Database (leave empty to use SQLite in dev)
DB_NAME=
DB_USER=postgres
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432

# Redis (leave empty to use in-memory channel layer in dev)
REDIS_URL=

# NowPayments
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=
NOWPAYMENTS_WEBHOOK_URL=
NOWPAYMENTS_EMAIL=
NOWPAYMENTS_PASSWORD=

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
ADMIN_EMAIL=

# Finnhub
FINNHUB_API_KEY=

# 2FA
TOTP_ENCRYPTION_KEY=
TOTP_ISSUER_NAME=Money Brokers
TOTP_STAFF_REQUIRED=False

# Challenge webhooks
CHALLENGE_WEBHOOK_SECRET=
CHALLENGE_STATUS_API_TOKEN=

# Sentry (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production

# Public base URL used in email links (no trailing slash)
# Development: http://127.0.0.1:8000
# Production:  https://yourdomain.com
SITE_URL=http://127.0.0.1:8000
